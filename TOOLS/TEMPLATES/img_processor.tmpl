
/*
 * 		STAR_NAME()
 *
 * Generated by stargen
 *
 * Image Processor
 * Notes:
 *    This is a star generated from the image processor template
 *    It will input a sample, process it and output the processed
 *    sample.
 * 
 *    This star simply inputs the sample and outputs it.
 *    
 * Programmer: 
 * Date: 
 * Modified: 
 *
 */


parameters

PARAMETERS

end

states

end

input_buffer
	image_t x;
end

output_buffer
	image_t y;
end


declarations
   	int i,j;
   	float fpixel;
   	image_t img;
end

initialization_code

  /*
   * set the output buffer cell size to size of int
   */	
  set_cellsize_out(0,sizeof(image_t));
  /*
   * set the input buffer cell size to size of int
   */	
  set_cellsize_in(0,sizeof(image_t));


end
 
main_code



while(it_in(0)) {
       img=x(0);
       
       /*
        * This is just to show haow to access the pixels
        */
        
        for(i=0; i< img.height; i++) {
        
           for (j=0; j<img.width; j++) {
              fpixel=img.image_PP[i][j];
           
           
           }
        }
   
   	   /*
	    * ready output buffer for sample
	    * check for overflow
	    */
	   if(it_out(0)) {
				KrnOverflow("STAR_NAME",0);
				return(99);
	   }
	   /*
	    * output the sample
	    */
	   y(0)=img;


}
    
end

wrapup_code

end


