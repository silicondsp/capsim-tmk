

/*
 * 		STAR_NAME()
 *
 * Generated by stargen
 *
 * Complex Processor
 * Notes:
 *    This is a star generated from the complex processor template
 *    It will input a sample, process it and output the processed
 *    sample.
 * 
 *    This star simply inputs the sample and outputs it.
 *    
 * Programmer: 
 * Date: 
 * Modified: 
 *
 */


parameters

PARAMETERS

end

states

end

input_buffer
	complex x;
end

output_buffer
	complex y;
end


declarations
   	int i;
   	complex sample;
end

initialization_code

  /*
   * set the output buffer cell size to size of complex
   */	
  set_cellsize_out(0,sizeof(complex));
  /*
   * set the input buffer cell size to size of complex
   */	
  set_cellsize_in(0,sizeof(complex));


end
 
main_code



while(it_in(0)) {
       sample=x(0);
   
   	   /*
	    * ready output buffer for sample
	    * check for overflow
	    */
	   if(it_out(0)) {
				KrnOverflow("STAR_NAME",0);
				return(99);
	   }
	   /*
	    * output the sample
	    */
	   y(0)=sample;


}
    
end

wrapup_code

end


