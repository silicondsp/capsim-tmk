

/*
 * 		STAR_NAME()
 *
 * Generated by stargen
 *
 * Fixed Point Processor
 * Notes:
 *    This is a star generated from the fixed point processor template
 *    It will input a sample, process it and output the processed
 *    sample.
 * 
 *    This star simply inputs the sample and outputs it.
 *    
 * Programmer: 
 * Date: 
 * Modified: 
 *
 */


parameters

PARAMETERS

end

states

end

input_buffer
	doublePrecInt x;
end

output_buffer
	doublePrecInt y;
end


declarations
   	int i;
   	doublePrecInt sample;
end

initialization_code

  /*
   * set the output buffer cell size to size of fixed point
   */	
  set_cellsize_out(0,sizeof(doublePrecInt));
  /*
   * set the input buffer cell size to size of fixed point
   */	
  set_cellsize_in(0,sizeof(doublePrecInt));


end
 
main_code



while(it_in(0)) {
       sample=indi(0,0);
       
       /*
        * sample structure components are:
        * 		sampleOut.lowWord
		*       sampleOut.highWord
		*/

   
   	   /*
	    * ready output buffer for sample
	    * check for overflow
	    */
	   if(it_out(0)) {
				KrnOverflow("STAR_NAME",0);
				return(99);
	   }
	   /*
	    * output the sample
	    */
	   outdi(0,0) = sample;
	   


}
    
end

wrapup_code

end


