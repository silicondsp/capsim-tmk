

/*
 * 		STAR_NAME()
 *
 * Generated by stargen
 *
 * Integer Processor
 * Notes:
 *    This is a star generated from the integer processor template
 *    It will input a sample, process it and output the processed
 *    sample.
 * 
 *    This star simply inputs the sample and outputs it.
 *    
 * Programmer: 
 * Date: 
 * Modified: 
 *
 */


parameters

PARAMETERS

end

states

end

input_buffer
	int x;
end

output_buffer
	int y;
end


declarations
   	int i;
   	int sample;
end

initialization_code

  /*
   * set the output buffer cell size to size of int
   */	
  set_cellsize_out(0,sizeof(int));
  /*
   * set the input buffer cell size to size of int
   */	
  set_cellsize_in(0,sizeof(int));


end
 
main_code



while(it_in(0)) {
       sample=x(0);
   
   	   /*
	    * ready output buffer for sample
	    * check for overflow
	    */
	   if(it_out(0)) {
				KrnOverflow("STAR_NAME",0);
				return(99);
	   }
	   /*
	    * output the sample
	    */
	   y(0)=sample;


}
    
end

wrapup_code

end


